type OrderPreDesignedHomeProps = {
  name: string;
  basePrice: number;
  options: { option: string; optionPrice: number }[];
};

const OrderPreDesignedHome = () => {
  return (
    <div>
      <h2></h2>
    </div>
  );
};

export default OrderPreDesignedHome;
